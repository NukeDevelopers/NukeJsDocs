# 变更记录

## Unreleased

- API version 更新为 `2`；
- 新增 Java/Android 反射 Bridge、ClassLoader、数组、重载和异常详情；
- 新增 `Java.hook()`，支持 before/after、参数、结果、异常和原方法调用；
- 删除旧 `nuke:host` API；
- 脚本日志同时进入运行时日志流和脚本目录日志文件；
- 开发者文档改为独立静态站点源码，不再随应用打包本地 Markdown。
